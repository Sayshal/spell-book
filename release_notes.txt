## Bug Fixes

- Fixed a crash when rendering spells whose range is a formula. The Spell List Manager and the Spell Book failed to open when the system's range formula resolution threw "ReferenceError: error is not defined".
- Range display now uses the system's prepared range label and falls back to resolving the formula with the actor's or spell's roll data.
