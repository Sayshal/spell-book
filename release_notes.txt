## Changes

- Removed the leftover Tidy5e Sheet and Chris's Premades entries from the module manifest's optional relationships block. Foundry does not read this key, so the entries were inert data; manifest cleanup with no behavior change.
