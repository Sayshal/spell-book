## Bug Fixes

- Fixed custom and merged spell lists disappearing from actors. A transient compendium load could wipe the saved original-to-custom list mappings and leave the originals hidden. Mappings are now pruned only against the fully loaded pack, and an original is unhidden when its mapping is genuinely removed.
