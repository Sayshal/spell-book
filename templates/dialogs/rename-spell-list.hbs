{{!--
/**
 * Rename Spell List Dialog Template
 *
 * PURPOSE:
 * Simple form dialog for renaming existing spell lists. Displays current name
 * as read-only reference and provides input for new name with validation.
 * Includes character limits and error handling for name validation.
 *
 * CONTEXT REQUIREMENTS:
 * @param {string} currentName - Current name of the spell list being renamed
 *
 * HELPER DEPENDENCIES:
 * @requires localize - Foundry VTT localization helper for text translation
 *
 * ACCESSIBILITY FEATURES:
 * - Semantic form structure with proper label associations
 * - Read-only current name field for reference
 * - Required and maxlength attributes for validation
 * - Hidden validation error section for dynamic error display
 * - Clear form groups for logical field organization
 *
 * CUSTOMIZATION POINTS:
 * - Current name displayed as disabled reference field
 * - New name field pre-populated with current value
 * - Character limit enforcement (100 characters)
 * - Validation error display for invalid names
 * - Required field validation for non-empty names
 *
 * INTEGRATION:
 * - Used in Spell List Manager for list renaming operations
 * - Connects to spell list validation and update systems
 * - Uses module localization keys from SPELLMANAGER.Rename namespace
 * - Supports spell list organization and management workflows
 */
--}}

<!-- Current Name Reference Field -->
<div class="form-group">
  <label for="current-name">{{localize "SPELLMANAGER.Rename.CurrentNameLabel"}}</label>
  <input type="text" id="current-name" name="currentName" value="{{currentName}}" readonly disabled>
</div>

<!-- New Name Input Field -->
<div class="form-group">
  <label for="new-name">{{localize "SPELLMANAGER.Rename.NewNameLabel"}}</label>
  <input type="text" id="new-name" name="newName" value="{{currentName}}"
    placeholder="{{localize 'SPELLMANAGER.Rename.NewNamePlaceholder'}}" maxlength="100" required>
</div>

<!-- Validation Error Display -->
<div class="form-group">
  <div class="validation-error" style="display: none;">
    <p class="error-message">{{localize "SPELLMANAGER.Rename.ValidationError"}}</p>
  </div>
</div>
