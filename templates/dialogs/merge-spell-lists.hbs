<div class='merge-spell-lists-dialog'>
  <!-- Spell Lists Multi-Select -->
  <div class="form-group">
    <label for="spell-lists-to-merge">{{localize "SPELLMANAGER.MergeLists.SpellListsLabel"}}</label>
    {{{formElements.spellListsMultiSelectHtml}}}
    <p class="hint">{{localize "SPELLMANAGER.MergeLists.SpellListsHint"}}</p>
  </div>

  <!-- Merged List Name Configuration (Required) -->
  <div class="form-group">
    <label for="merged-list-name">{{localize "SPELLMANAGER.MergeLists.MergedListNameLabel"}} <span class="required">*</span></label>
    {{{formElements.mergedListNameInputHtml}}}
    <p class="hint">{{localize "SPELLMANAGER.MergeLists.MergedListNameRequired"}}</p>
  </div>

  <!-- Source List Visibility Option -->
  <div class="form-group">
    <label class="checkbox">
      <span class="label-text">{{localize "SPELLMANAGER.MergeLists.HideSourceListsLabel"}}</span>
      {{{formElements.hideSourceListsCheckboxHtml}}}
    </label>
    <p class="hint">{{localize "SPELLMANAGER.MergeLists.HideSourceListsHint"}}</p>
  </div>

  <!-- Validation Error Display -->
  <p class="validation-error" style="color: red; display: none;">{{localize "SPELLMANAGER.MergeLists.ValidationError"}}
  </p>
</div>
