{{!--
/**
 * Merge Spell Lists Dialog Template
 *
 * PURPOSE:
 * Form dialog for merging two spell lists into a new combined list. Provides
 * source list selection, name configuration, and options for source list
 * visibility management. Includes validation and guidance for spell list
 * merging operations with error handling.
 *
 * CONTEXT REQUIREMENTS:
 * @param {Object} formElements - Pre-rendered form element HTML
 * @param {string} formElements.sourceListSelectHtml - Source list dropdown HTML
 * @param {string} formElements.copyFromListSelectHtml - Secondary list dropdown HTML
 * @param {string} formElements.mergedListNameInputHtml - Merged list name input HTML
 * @param {string} formElements.hideSourceListsCheckboxHtml - Hide sources checkbox HTML
 *
 * ACCESSIBILITY FEATURES:
 * - Semantic form structure with proper label associations
 * - Form groups for logical organization of related fields
 * - Checkbox label with proper association and descriptive text
 * - Hint text providing guidance for form fields
 * - Hidden validation error section for dynamic error display
 *
 * CUSTOMIZATION POINTS:
 * - Source list selection from available spell lists
 * - Custom naming for merged list results
 * - Optional hiding of source lists after merge
 * - Validation error display for same-list selection
 * - Guidance hints for user decision-making
 *
 * INTEGRATION:
 * - Used in Spell List Manager for list combination operations
 * - Connects to spell list merging and validation systems
 * - Uses module localization keys from SPELLMANAGER.MergeLists namespace
 * - Supports spell list organization and management workflows
 */
--}}

<div>
  <!-- Source List Selection -->
  <div class="form-group source-list">
    <label for="source-list">{{localize "SPELLMANAGER.MergeLists.SourceListLabel"}}</label>
    {{{formElements.sourceListSelectHtml}}}
  </div>

  <!-- Secondary List Selection -->
  <div class="form-group copy-from-list">
    <label for="copy-from-list">{{localize "SPELLMANAGER.MergeLists.CopyFromListLabel"}}</label>
    {{{formElements.copyFromListSelectHtml}}}
  </div>

  <!-- Merged List Name Configuration -->
  <div class="form-group merged-list-name">
    <label for="merged-list-name">{{localize "SPELLMANAGER.MergeLists.MergedListNameLabel"}}</label>
    {{{formElements.mergedListNameInputHtml}}}
    <p class="hint">{{localize "SPELLMANAGER.MergeLists.MergedListNameNotes"}}</p>
  </div>

  <!-- Source List Visibility Option -->
  <div class="form-group hide-source-lists">
    <label for="hide-source-lists" class="checkbox-label">
      {{{formElements.hideSourceListsCheckboxHtml}}}
      <span>{{localize "SPELLMANAGER.MergeLists.HideSourceListsLabel"}}</span>
    </label>
    <p class="hint">{{localize "SPELLMANAGER.MergeLists.HideSourceListsHint"}}</p>
  </div>

  <!-- Validation Error Display -->
  <p class="validation-error" style="color: red; display: none;">{{localize "SPELLMANAGER.MergeLists.SameListError"}}
  </p>
</div>
