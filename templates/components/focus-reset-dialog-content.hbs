{{!--
/**
 * Focus Reset Dialog Content Template
 *
 * PURPOSE:
 * Template for focus settings reset confirmation dialog content.
 * Provides structured content for warning users about the reset operation.
 *
 * CONTEXT REQUIREMENTS:
 * This template uses static localization and doesn't require dynamic context.
 *
 * INTEGRATION:
 * - Used by FocusSettingsDialog.resetFocuses() method
 * - Uses SPELLBOOK.FocusSettings.ResetDialog localization namespace
 */
--}}

<p>{{localize 'SPELLBOOK.FocusSettings.ResetDialog.Content'}}</p>
<p><strong>{{localize 'SPELLBOOK.FocusSettings.ResetDialog.Warning'}}</strong></p>
